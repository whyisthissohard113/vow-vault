# Worker Handoff Contract

Every completion creates `docs/worker-handoffs/YYYY-MM-DD-<worker>-<short-task>.md` with Worker, Task, Status, Completed, Files changed, Database changes, API/contracts changed, Tests/results, Environment changes, Known issues, Next worker, and Decisions required.
